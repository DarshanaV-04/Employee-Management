# .env file

# Database Configuration
DB_HOST=localhost           # Database host (usually localhost for local dev)
DB_USER=root                # Your MySQL username
DB_PASSWORD=root # Your MySQL password (leave blank if no password)
DB_NAME=employee_management # Name of your database

# Server Configuration
PORT=5000
                 # The port the backend will run on
